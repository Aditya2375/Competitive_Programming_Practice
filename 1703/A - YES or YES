#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int t;
    cin >> t;
    while (t--) {
        string s;
        cin >> s;
        bool ok = true;
        string target = "YES";
        for (int i = 0; i < 3; i++) {
            if (toupper(s[i]) != target[i]) {
                ok = false;
                break;
            }
        }
        cout << (ok ? "YES" : "NO") << "
";
    }
    return 0;
}